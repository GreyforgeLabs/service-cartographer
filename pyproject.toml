[build-system]
requires = ["setuptools==68.1.2"]
build-backend = "setuptools.build_meta"

[project]
name = "service-cartographer"
version = "0.2.0"
description = "Read-only workflow inventory for services, cron jobs, wrappers, repositories, and environment files."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "AGPL-3.0-only" }
authors = [
  { name = "Greyforge", email = "admin@greyforge.tech" },
]
keywords = ["systemd", "cron", "inventory", "devops", "audit", "workflow"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: System Administrators",
  "License :: OSI Approved :: GNU Affero General Public License v3",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: System :: Systems Administration",
  "Topic :: Utilities",
]
dependencies = []

[project.optional-dependencies]
dev = [
  "build==1.4.2",
  "pytest==9.0.2",
  "ruff==0.15.0",
  "twine==6.1.0",
]

[project.scripts]
service-cartographer = "service_cartographer.cli:main"

[project.urls]
Homepage = "https://greyforge.tech"
Repository = "https://github.com/GreyforgeLabs/service-cartographer"
Issues = "https://github.com/GreyforgeLabs/service-cartographer/issues"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]
